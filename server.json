{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "ai.limitguard.api/trust-intelligence",
  "title": "Limitguard Trust Intelligence",
  "description": "Entity verification, sanctions screening, and trust scoring for AI agents via x402 micropayments.",
  "version": "2.2.5",
  "websiteUrl": "https://limitguard.ai",
  "repository": {
    "url": "https://github.com/jwconsultancyteam/limitguard-mcp",
    "source": "github"
  },
  "packages": [],
  "remotes": [
    {
      "type": "streamable-http",
      "url": "https://api.limitguard.ai/mcp",
      "headers": [
        {
          "name": "Authorization",
          "description": "Bearer lg_live_... -- the full header value, 'Bearer ' included. Free key: POST https://api.limitguard.ai/v1/keys/create with an email.",
          "isRequired": true,
          "isSecret": true
        }
      ]
    }
  ],
  "tools": [
    {
      "name": "check_entity",
      "description": "Full trust intelligence check on a business entity. Returns trust score (0-100), risk level, and recommendation.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "entity_name": {
            "type": "string",
            "description": "Full legal name of the entity"
          },
          "country": {
            "type": "string",
            "description": "ISO 3166-1 alpha-2 country code"
          },
          "kvk_number": {
            "type": "string",
            "description": "Optional Dutch KVK number (8 digits)"
          },
          "domain": {
            "type": "string",
            "description": "Optional company website domain"
          }
        },
        "required": [
          "entity_name",
          "country"
        ]
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      }
    },
    {
      "name": "verify_wallet",
      "description": "Screen a wallet: OFAC SDN address match, on-chain signals (contract check, native and USDC balance, transaction count, first seen on Base) and named risk rules with up to 3 advice items. On Base, also reports any ERC-8004 agent the wallet owns and its open on-chain reputation as descriptive signals, never scored. Free ($0). Supports EVM and Solana addresses.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "wallet_address": {
            "type": "string",
            "description": "Blockchain wallet address"
          },
          "chain_id": {
            "type": "string",
            "description": "CAIP-2 chain ID (default: eip155:8453 for Base); base, eth, solana, btc also accepted"
          }
        },
        "required": [
          "wallet_address"
        ]
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      }
    },
    {
      "name": "get_risk_score",
      "description": "Quick risk assessment without full trust check. Focuses on risk signals only.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "entity_name": {
            "type": "string",
            "description": "Full legal name of the entity"
          },
          "country": {
            "type": "string",
            "description": "ISO 3166-1 alpha-2 country code"
          }
        },
        "required": [
          "entity_name",
          "country"
        ]
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      }
    },
    {
      "name": "get_compliance_report",
      "description": "Per-entity report built from one real check: registry identity, sanctions and PEP screens, domain signals, risk score with the rules that fired, correlations, at most 3 findings, a per-source status table (ok / unavailable / error) and a report hash.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "entity_name": {
            "type": "string",
            "description": "Full legal name (with country; omit when sending check_id)"
          },
          "country": {
            "type": "string",
            "description": "ISO 3166-1 alpha-2 country code"
          },
          "kvk_number": {
            "type": "string",
            "description": "Optional Dutch KVK number (8 digits)"
          },
          "cbe_number": {
            "type": "string",
            "description": "Optional Belgian CBE number (0XXX.XXX.XXX)"
          },
          "vat_number": {
            "type": "string",
            "description": "Optional EU VAT number"
          },
          "domain": {
            "type": "string",
            "description": "Optional company website domain"
          },
          "iban": {
            "type": "string",
            "description": "Optional IBAN (never stored raw)"
          },
          "wallet_address": {
            "type": "string",
            "description": "Optional crypto wallet address (format check only)"
          },
          "wallet_chain": {
            "type": "string",
            "description": "Optional chain for wallet_address (eth, base, btc, sol, ...)"
          },
          "check_id": {
            "type": "string",
            "description": "Id of a check from an earlier report, instead of identifiers"
          }
        },
        "required": []
      },
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": true
      }
    }
  ]
}
