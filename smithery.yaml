# Generated by limitguard-ai scripts/sync_public_repos.py (#560); edit the source, not this file.
name: "team-mehs/limitguard"
description: "Entity verification, sanctions screening, and trust scoring for AI agents via x402 micropayments."
startCommand:
  type: "http"
  url: "https://api.limitguard.ai/mcp"
  configSchema:
    type: "object"
    properties:
      apiKey:
        type: "string"
        description: "Authorization header value, including the 'Bearer ' prefix (format: Bearer lg_live_...). Get a key at https://limitguard.ai via POST /v1/keys/create."
        x-from:
          header: "bearer-token"
        x-to:
          header: "Authorization"
    required: ["apiKey"]
